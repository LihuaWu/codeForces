#include <bits/stdc++.h>
using namespace std;
// TODO : finish it this tonight.
int main(int argc, char **argv) {

    std::string a, b;
    cin >> a >> b;
    map<char, int> v;
    size_t n = a.size(), m = b.size();
    for (int i = 0; i < n; i++) { v[a]++; }

    return EXIT_SUCCESS;
}
