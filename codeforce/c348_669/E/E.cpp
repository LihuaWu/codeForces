#include <bits/stdc++.h>
using namespace std;

map<int, pair<int, int>> count;
int main(int argc, char **argv) {
    int n;
    cin >> n;
    for (int i = 0; i < n; i++) {
        int a, t, x;
        cin >> a >> t >> x;
        if (a == 1) {
            if (count.count(x) == 0) {
                count[x]
            }


        } else if (a == 2) {
        } else if (a == 3) {
        }
    }
    return EXIT_SUCCESS;
}
